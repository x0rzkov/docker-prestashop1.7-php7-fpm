---
services:
  web:
    interactive: true
    tty: true
    volume: ["./app:/app"]
    workdir: /app
    share-ssh-socket: true
    cmd: ["bash"]

  #prestashop:
  #  interactive: true
  #  tty: true
  #  volume: ["./app:/app"]
  #  workdir: /app
  #  share-ssh-socket: true
  #  cmd: ["bash"]

commands:
  test: run crane make test

accelerated-mounts:
  web:
    uid: 1000
    gid: 1000
  #prestashop:
  #  uid: 1000
  #  gid: 1000